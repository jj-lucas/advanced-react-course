describe('sample test 101', () => {
	console.log('I am running some test')
	it('works as expected', () => {
		expect(1).toEqual(1)
	})
	fit('works as expected', () => {
		expect(1).toEqual(1)
	})
})
